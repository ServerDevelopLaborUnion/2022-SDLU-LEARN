websocket.OnOpen += (sender, e) =>               웹소켓서버 열기
Debug.Log("서버에 연결");                         출력
websocket.OnMessage += (sender, e) =>            서버로 부터 메세지가 옴
        {
            Debug.Log(e.Data);
        };
public void SendChat()                           서버로 메세지보내기
    {
        websocket.Send(input.text);
    }
    websocket.OnMessage += (sender, e) =>        서버에있는 메세지 출력
        {
            Debug.Log(e.Data);
        };
        